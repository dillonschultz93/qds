packages:
  - "packages/*"
  - "apps/*"
# Both are part of the @web/test-runner toolchain and need their postinstall:
# esbuild links a platform-native binary and simply cannot run without it.
allowBuilds:
  esbuild: true
  rs-module-lexer: true
